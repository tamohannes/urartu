defaults:
  - _self_
  - action_config: default_action_config
  - slurm: default_slurm
  - aim: default_aim
  - machine: local

action_name: ???
debug: ???

# Optional pipeline configuration (only used by pipeline actions)
pipeline_config: {}

run_dir: ".runs/${action_name}/${now:%Y-%m-%d}_${now:%H-%M-%S}"

hydra:
  sweep:
    dir: "${run_dir}_multirun"
