#!/bin/bash
# Auto-generated conda initialization script
# This script is cached to speed up remote setup

if [ -f "{CONDA_INIT_PATH}" ]; then
    source {CONDA_INIT_PATH}
else
    echo "ERROR: Conda initialization script not found at {CONDA_INIT_PATH}" >&2
    exit 1
fi

